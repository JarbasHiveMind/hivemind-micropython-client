# CPython-side test dependencies.
#
# This is a MicroPython package (see package.json) installed on-device via
# `mip`; it has no CPython packaging and no pip runtime dependencies. These
# requirements exist ONLY so the test suite can run under CPython in CI:
#
#   * the reference HiveMind stack, used by the conformance/integration tests
#     to validate byte-for-byte Protocol-V1 interop, and
#   * the optional accelerators the client uses on CPython (cryptography for
#     AES-GCM/ChaCha20, websockets for transport, z85base91 for Z85/B91).
#
# Floored to the HiveMind 2.x protocol stack. Prereleases are pinned as the
# minimum version so pip resolves them without --pre.
hivemind-bus-client>=0.9.2a1
ovos-bus-client>=2.0.0a3
# >=1.0.2a5 for poorman_handshake.noise, the protocol-v3 Noise reference the
# interop tests validate against (fixture generation + mock hub responder)
poorman_handshake>=1.0.2a5
z85base91>=0.0.6a6
cryptography
websockets
pytest
pytest-cov
pytest-asyncio
